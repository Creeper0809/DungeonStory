# Progress

## 2026-07-27
- Created isolated implementation plan for the Research Tree conversion.
- Preserved the existing dirty worktree as the baseline.
- Started Phase 1 API and compile-state inspection.
- Logged one failed baseline read caused by an incorrect assumed blueprint model path; switching to symbol-first discovery.
- Confirmed Unity MCP connection and a clean baseline Console (0 errors, 0 warnings).
- Confirmed legacy shop-purchase auto-enqueue and generic feature-card UI are the primary replacement points.
- Added the project model, project catalog, graph validation, deterministic layered layout, Blueprint stock category, and blueprint-to-project metadata.
- Generated and validated all 24 project assets.
- Moved the seven legacy blueprint unlock collections to their target projects and assigned stable target project IDs.
- Added `BuildingResearchArchiveAbility` to Q03 with capacity 8 while keeping Q01/Q03 baseline unlocked.
- Added V3 research save-section DTOs while keeping global save V16, including legacy blueprint task/completion migration and physical blueprint materialization.
- Added full-screen `ResearchTreeWindow`: deterministic connectors, pan, cursor-centered zoom, search dimming, field filters, project details, queue drag reorder, desktop inspector, portrait bottom sheet, and optional auto-pause.
- Replaced the Research tab surface kind with `ResearchTree` and removed the obsolete card presenter registration and source.
- Changed shop purchases and offense rewards so blueprints arrive as unique physical items at the drop zone and never auto-enqueue research.
- Fixed required-blueprint validation for automatically inserted prerequisite paths.
- Fixed `[SerializeReference]` unlock migration with deep copies; project assets now own rewards and blueprint assets retain no duplicate unlocks.
- Research Tree 24/100/250 layout scenarios and updated P1 research regressions pass with Console Error/Warning 0.
- Fixed the full-screen tree's `RectMask2D` dependency and graph-root anchoring; desktop and portrait pointer verification now passes for 24 nodes, queue drag, pan, zoom, search, close, and exact pause-state restore.
- Added `ResearchTreePlayModeVerifier` captures and a PASS report with Console Error/Warning 0.
- Replaced remaining product research-summary authority with project queue/completion/progress state.
- Updated first-run, P0 surface, natural-run, and offense reward regressions so blueprint purchases create physical items without auto-enqueuing legacy tasks.
- Recompiled the project after regression updates with Console Error/Warning 0.
- Fixed archive-room recognition for modular Q03 parts by resolving their occupied cells.
- Extended blueprint archive status to recognize blueprints carried by active characters.
- Isolated the first-run verifier from persistent completed-run metadata and removed its race with already reserved AI haul actions.
- Verified the physical blueprint loop end to end: pointer purchase, DropZone spawn, AI haul, valid archive, queue creation, real research work, and project completion.
- Updated P0 regression coverage to inspect the dedicated research tree instead of the removed card surface.
- Removed the duplicate `OwnerCommandController` fixture from `SampleScene` and fixed the verifier's AI component construction order.
- Fixed feedback-bubble pooling during actor deactivation so regression teardown and real despawn paths do not reparent children during hierarchy activation changes.
- Research Tree PlayMode verification passes at 1600x900 and 900x1600 with pointer pan, zoom, search, queue drag, close, and optional pause restoration.
- P0 UI, P1/P2 UI (21/21), Unified UI, research/shop/offense/save EditMode regressions, and the physical blueprint flow all pass with Console Error/Warning 0.
- Phase 6 verification complete; editor left stopped in EditMode.
