# DungeonStory Agent Guide

## 적용 범위

- 이 문서는 Unity 저장소 `DungeonStory`의 공통 지침이다. 실제 수정할 저장소와 적용되는 하위 `AGENTS.md`부터 확인한다.
- 현재 사용자 요청과 상위 지침을 우선한다. 다른 저장소의 지침·과거 보고를 현재 작업 권위로 혼용하지 않는다.
- 처음 작업할 때 끝까지 읽고, 같은 맥락에서 변경이 없으면 반복해서 읽지 않는다.
- 아래 참조 문서는 해당 작업에 필요한 것만 연다. 링크가 있다는 이유로 모두 읽거나 실행하지 않는다.
- 기존 사용자 변경, 미저장 씬, 저장 데이터, QA 증거를 보존한다. 파괴적 작업과 승인 범위 확대는 임의로 진행하지 않는다.

## 우선순위와 범위

- 먼저 크래시·데이터 손상·진행 불가·핵심 UI/AI 단절 등 현재 플레이를 막는 P0를 해결한다.
- 기본 흐름은 작동하는 수직 슬라이스 구현 → 단일 권위 확인 → 현재 소스 컴파일·핵심 테스트다.
- P1 품질 개선과 P2 전수 조합·대규모 성능·미래 콘텐츠 증명은 필요한 경우에만 별도 범위로 잡는다.
- 사용자가 이미 승인한 기능과 최종 통합 요구는 유지한다. P0가 끝났다고 전체 목표가 끝난 것으로 보고하지 않는다.
- 작은 수정에는 작은 계획과 검증을 사용한다. 구현보다 큰 추상화·문서·감사 체계를 선제 도입하지 않는다.

## 실패와 폴백

- 기본적으로 폴백을 추가하지 않는다. 실패 이유와 불가 상태를 호출자·로그에 명시한다.
- 설계나 사용자 요청, 의도된 호환 경로에 폴백이 필요하면 원인·원래 행동·대체 행동·영향 대상을 기록한다.
- 게임 동작을 바꾸는 폴백에는 원래 실패와 대체 경로의 표적 회귀 검증을 둔다.
- 잘못된 데이터·목적지 누락·경로 탐색 실패를 임의 행동, 기본값 또는 가짜 결과로 덮지 않는다.
- 실패를 숨기는 assert 완화, 근거 없는 timeout 연장, 복원 데이터 누락과 폴백 연쇄를 만들지 않는다.

## 단일 권위와 구현

- ScriptableObject는 불변 정의, Runtime은 가변 상태, DTO는 저장 표현을 소유한다.
- 상태 변경은 해당 소유자의 명령으로 수행한다. UI·캐시·ViewModel·저장 DTO에 두 번째 쓰기 권위를 만들지 않는다.
- 저장하지 않는 파생값은 원본과 revision으로 재계산한다. 실패한 명령·복원이 상태를 부분 변경하지 않게 한다.
- 새 효과·명령은 실제 생산자 → 권위 상태 변화 → 소비자와 필요한 UI/AI·저장 경로까지 연결한다.
- 정의·getter·Editor fixture만 존재하는 상태를 플레이 기능 완료로 세지 않는다.
- 기존 capability/strategy를 재사용하고, 공용 코어에 콘텐츠 ID별 분기나 미래용 빈 추상화를 늘리지 않는다.
- 하위 계산·검증과 Unity 조립·표현을 분리한다. 줄 수를 맞추기 위한 helper/partial 분할은 하지 않는다.
- 씬·프리팹·에셋·인스펙터 연결을 바꿀 때는 영향과 사용자 수동 작업 여부를 먼저 알린다.

## 작업 흐름

1. 요청과 관련 원본을 확인하고 수정 범위·완료 조건을 정한다.
2. 상태·저장·다중 도메인 변경이면 소유자, 명령/조회, 실패, 호환과 검증 경계를 먼저 확정한다.
3. 독립적인 쉬운 작업을 배정하고, 복잡한 구현은 메인이 수행한다.
4. 관련 수정 묶음을 마친 뒤 위험에 맞는 검증을 실행한다.
5. 실제 결과와 남은 문제를 짧게 보고한다. 코드 존재·컴파일 PASS·실행 PASS를 구분한다.

- 여러 단계 작업은 기존 `task_plan.md`에 현재 단계, `findings.md`에 결정, `progress.md`에 결과·증거 경로만 기록한다.
- 같은 내용을 세 파일에 복사하거나 도구 호출·대기마다 일지를 추가하지 않는다.

## 조사와 검증

- 정의·호출처 조사에는 먼저 [지식베이스 쿼리](Tools/Documentation/query_knowledge_base.py)를 사용한다. [사용법](docs/dev/verification.md)의 좁은 query/area부터 시작한다.
- stale이면 원본 C#/에셋으로 확인한다. 인덱스는 탐색 단서이며 실행 증거가 아니다. 생성물을 직접 편집하지 않는다.
- 고아·연결·크기 검사는 [검증 도구 안내](docs/dev/verification.md)의 해당 도구를 사용한다. 수작업 전수 표를 매 작업마다 만들지 않는다.
- 도구가 다루지 않는 변경 경계는 원본과 실제 호출 경로를 확인한다. 문자열 검색이나 정적 검사만으로 모든 런타임 연결을 보장하지 않는다.
- 테스트는 변경한 동작과 대표 실패 경계를 다룬다. 저장·취소·재시도 위험이 있으면 그 경계를 포함한다.
- 통제 fixture, 실제 UI/AI 실행, 장기 성능 측정을 구분한다. 실행하지 않은 검사를 PASS로 보고하지 않는다.
- 기존 PASS는 관련 소스·계약 변화나 실제 실패가 있을 때만 재검증한다. 무관한 전체 컴파일·Play·감사를 반복하지 않는다.
- 문서·지침만 바뀌면 관련 링크·내용·diff로 검증한다. Unity 실행과 전체 데이터 재생성을 추가하지 않는다.
- 영향받는 생성 인덱스는 관련 코드/콘텐츠 배치 마감에 한 번 갱신한다. 읽기 전용 조사에서는 재생성하지 않는다.

## Unity 배타적 작업 창

- `SOURCE_EDIT`와 `UNITY_VERIFY`는 겹치지 않는다. 메인이 배치와 Unity 담당자 한 명을 지정한다.
- 소스·테스트·asmdef·에셋 편집 전 담당자가 Play 종료, 실행 명령 없음, 컴파일/import/reload 종료를 확인해 `EDIT_READY`로 알린다.
- `SOURCE_EDIT` 동안 명시적 Refresh·컴파일·Play·Unity 명령을 시작하지 않는다. 자동 import 설정도 임의 변경하지 않는다.
- 모든 작성자가 `READY/FROZEN`을 알린 후 편집 창을 닫고 `UNITY_VERIFY`를 연다.
- 검증 중에는 소스를 수정하지 않는다. 발견한 수정은 다음 편집 창에서 처리한다.
- Unity 호출은 한 번에 하나만 실행한다. timeout/연결 끊김을 종료로 간주하지 않고 기존 handle·프로세스·로그를 확인한다.
- 현재 소스의 import·컴파일 결과를 확인한다. `IsCompiling=false`, 콘솔 오류 0 또는 구형 DLL 실행만으로 성공을 선언하지 않는다.
- 세션 재개 시 과거 편집/검증 허가는 만료된다. 현재 상태를 확인하고 미저장 변경이 있는 Editor를 임의 종료하지 않는다.
- 연결 방식과 MCP 사용 여부는 현재 사용자 요청을 따른다. 비활성 서버를 임의 등록·재활성화하지 않는다.
- CLI 예시와 연결 확인 범위는 [Unity 실행 안내](docs/dev/unity-workflow.md)에 있다.

## 메인·서브 에이전트

| 작업 | 담당 |
|---|---|
| 복잡한 구현·설계·디버깅, 상태/저장/수명/다중 도메인, 어려운 테스트 설계·구현, 최종 통합 | 메인 직접 |
| 계약·수정 범위가 확정된 독립 구현·테스트 설계 중 비교적 판단이 필요한 작업 | `gpt-5.6-sol` / `xhigh` (ExtraHigh) |
| 기존 패턴의 국소 구현·UI 연결·데이터 작성·쉬운 테스트 설계와 구현 | `gpt-5.6-terra` / `xhigh` (ExtraHigh) |
| 입력·정답·수정 위치가 확정된 기계적 치환·행 입력·극단순 테스트 | `gpt-5.6-luna` / `max` |

- 메인은 계약·수용 조건·소유 파일을 먼저 정하고 최종 diff와 증거를 검토한다.
- 파일별 작성자는 하나다. 공용 계약·composition root·저장은 단일 담당자가 통합한다.
- 쉬운 테스트는 입력·관측 권위·기대 결과·실패 조건을 지정해 위임한다. 복잡한 setup과 의미 판단은 메인이 맡는다.
- 계약 밖 문제나 충돌은 최소 재현·증거와 함께 반환한다. 서브 에이전트는 범위를 넓히거나 재위임하지 않는다.
- 작은 작업은 메인이 직접 처리한다. 슬롯을 채우려고 분할하지 않는다.
- Unity를 쓰는 검증은 지정 담당자만 실행하고, 서브 에이전트는 배정된 정적 점검·표적 테스트만 수행한다.

## 토큰·조정 비용

- 난이도에 맞는 가장 가벼운 모델을 선택한다.
- 독립 작업만 병렬화하고 실제 슬롯 이내 최소 인원을 사용한다. 같은 문제를 여러 모델에 경쟁적으로 맡기지 않는다.
- `fork_turns="none"`과 짧은 작업 패킷을 기본으로 한다. 모델/effort 지정 시 전체 이력 fork를 사용하지 않는다.
- 패킷에는 저장소 절대 경로, 적용 지침, 목적, 소유 파일, 계약, 완료 조건과 검증 책임을 담는다.
- 전체 로그·파일을 복사하지 않고 관련 심볼·변경 diff·오류 발췌·증거 경로를 전달한다.
- 같은 계약의 후속 작업은 기존 담당자를 재사용한다. 문맥이 달라지면 확정 사실만 새 담당자에게 넘긴다.
- 담당자는 자체 점검 후 `READY/FROZEN`으로 반환한다. 충돌·차단 외에는 반복 독려·중간 리뷰·중복 조사를 피한다.
- 재시도는 원인을 좁힌 뒤 수정된 접근으로 한다.

## 조건부 참조

| 작업 | 읽을 문서 |
|---|---|
| 수치·경제·난이도·진행·실효 효율 변경 | [전체 게임 밸런스 기준](docs/game-design/whole-game-balance-baseline.md)의 관련 영역과 변경 기록 |
| 상태·저장·다중 도메인 계약 변경 | [아키텍처 변경 안내](docs/dev/architecture-review.md)와 연결된 해당 계약 |
| 정적 감사·지식베이스 조회·문서 검사 | [검증 도구 안내](docs/dev/verification.md) |
| Unity Editor 제어·컴파일·Play | [Unity 실행 안내](docs/dev/unity-workflow.md) |
| 사용자가 명시적으로 요청한 프레임워크 확장 인증 | [확장 인증 절차](docs/dev/architecture-review.md#확장-인증명시적-요청-시) |

- synthetic canary·전수 fault/seed·형식적 확장 증명은 일반 P0와 작은 수정의 기본 게이트가 아니다.
- 기존에 별도로 승인된 통합·밸런스 검증은 해당 계약에 따라 완료한다.

## 완료 보고

- 기본 보고는 무엇이 바뀌었는지, 무엇을 확인했는지, 남은 문제가 무엇인지로 끝낸다.
- 실제로 확인한 범위만 완료로 말한다. 실패·미실행·도구가 다루지 않는 범위는 필요한 만큼 명시한다.
- 감사가 요청된 경우에만 해당 수치·manifest를 붙인다. 매 응답에 고아 수·6항목 표·전수 증거를 강제하지 않는다.
