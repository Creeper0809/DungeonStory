# DungeonStory Current Progress

## 2026-09-20 — production-entry atomicity correction started

- Re-read the persistent plan/findings/progress and opened a new source-edit sub-batch for the five P1 re-review defects.
- No Unity process or MCP execution is part of the source-edit phase. Next: inspect the exact owner snapshot/restore contracts, implement the narrow fixes, and add real-path fault regressions before verification.

## 2026-09-20 — ledger economy producer transaction sub-batch started

- READY/FROZEN: connected `AutoProcurementResult`, `BuildingRetailPurchaseCommitResult`, `FacilityCrimeEvent`, `OperatingDayReportEvent`, and both live `StockSupplyResult` producers to explicit migrated-producer owner transactions. Final production/non-Editor symbol coverage is 1/1/1/1/2 respectively, and global `git diff --check` exits 0 with line-ending warnings only.
- Source changes are limited to eight owner/dedicated-seam files. Common enum/catalog/manifest/runner files and Editor fixtures were not changed by this sub-batch. No Unity, compile, test, MCP, inventory, or manifest command ran.
- Read the complete root `AGENTS.md`, existing planning state, and the applicable architecture/verification guidance.
- Confirmed the shared worktree is heavily dirty and opened a narrowly owned `SOURCE_EDIT / EDIT_READY` pass for only the five assigned producer families.
- Tried the mandated narrow knowledge-base lookup; it reported stale state and timed out partway through the five-symbol batch. Recorded the failure and switched to current-source inspection without regenerating indexes.
- Located all production/non-Editor references for the five assigned result types and the existing migrated-producer transaction pattern. No source edits have been made yet.
- Read the migrated transaction contract/runtime and the already integrated facility-shop owner as the baseline for reserve, commit, exact owner rollback, and post-commit observer isolation.
- Inspected auto-procurement and stock-supply authority seams. Confirmed the physical stack owner supports whole-state snapshot/restore, avoiding a new durable child outbox for these bounded mutations.
- Mapped both live stock-result producers and their observer timing. Warehouse delivery is directly reversible; offense stock rewards require a small dedicated owner adapter because their current sink abstraction hides physical rollback/current-day dependencies.
- Traced retail commit to `AbilityShopping.BuyItem` and identified the exact physical lot operation ID and current clock as the stable transaction identity/day inputs. The outer shop coroutine is only a consumer and should remain unchanged.
- Traced shoplifting to the same exact-lot/external-sink owner seam and identified the synchronous event/alert/activity calls that must be post-commit isolated.
- Confirmed a deterministic pre-mutation retail unit operation identity can be derived within `Shop`, so no common shop contract/catalog change is needed.
- Mapped the complete operating-day mutation sequence and moved its report event conceptually to a post-commit observer boundary; exact rollback requires all domain/economy owners, not only the public report snapshot.
- Confirmed the operating-day domain already supports an exact internal deep clone; only a narrow owner capture/restore seam is needed, with no save DTO or common ledger framework change.
- Chose additive injection seams that preserve existing direct fixture signatures while making each live producer explicitly require the ledger transaction/current-day dependency.
- Verified offense DI can supply the needed stock-result transaction/physical dependencies without touching the shared registration file.
- Confirmed unique retail externalization is irreversible under the current contract, so retail purchase/crime must use the migrated commit itself as the pre-sink durable boundary rather than attempting an impossible post-sink rollback.
- Checked ledger statuses and rejected using a permanently partial outcome as a fake pending state; next inspection focuses on whether the physical retail operation already has a durable idempotent receipt that can serve as the child boundary.
- Reused the established split: exact snapshot rollback for stock/operating-day mutations, and a durable pre-child boundary for irreversible retail externalization. No common migrated-producer framework changes are required.
- Verified the intended owner files have no pre-existing diff. Implementation can now proceed without overwriting another agent's work.
- Implemented the auto-procurement and both live stock-supply transaction seams. Scoped whitespace checks pass; no Unity, compile, tests, or MCP were run.
- A combined retail/crime patch wrapper failed before editing. Recorded the error; source remains unchanged for those three files and the edit is continuing in smaller patches.
- Implemented retail purchase, shoplifting, and operating-day report transaction seams. Scoped `git diff --check` is clean apart from line-ending conversion warnings; no Unity, compile, tests, or MCP were run.
- Performed a compile-risk review of referenced properties/types and corrected the planned subject identity rule before final static checks.
- Audited caller fallback behavior and queued fail-loud corrections so ledger failures cannot be mistaken for ordinary unavailable stock/reward outcomes.
- Applied fail-loud corrections and snapshot-acquisition cancellation guards. Targeted whitespace checks remain clean apart from Git line-ending warnings.

## 2026-09-20 — one goal created for all 57 pending outcome producers

- Created the active goal to connect all 57 remaining `transaction-integration-pending` producers in one continuous implementation batch.
- Replaced per-row compile/full-save/manifest promotion with owner-group source integration followed by one consolidated compile, focused/fault/replay, full-world save and final inventory gate.
- Read the current Unity `AGENTS.md`, planning files and applicable planning/Unity scripting guidance. Confirmed no Unity-family process is running and neither Unity MCP nor `dungeon-player` MCP is in use.
- No gameplay source, manifest disposition, commit or push has changed yet in this goal.
- Extracted the exact pending inventory from the four disposition files and confirmed the count is 57 (13 + 14 + 18 + 12).
- Began the shared-architecture pass. Group 3, 4 and 5 producer/owner mapping is delegated read-only in parallel; the main agent is mapping group 2 and owns the common transaction/save design.
- Confirmed the existing registry's one-concrete-receipt-type mapping and the approved requirement that observer-only recording cannot close a pending row. The next design decision is a reusable typed-wrapper/common-writer layer, not a generic after-the-fact event logger.
- Mapped the group 2 production seams and confirmed that one shared owner-revision/save component plus domain-specific rollback adapters can cover the cluster without changing `GameEventBus` globally.
- Completed the exact group 3/4/5 read-only owner map, so all 57 rows now have an identified production seam and transaction-sharing decision.
- Added the closed 57-type migrated-producer catalog, constrained dynamic adapter/descriptor coverage, fixed-buffer receipt, deterministic owner revision runtime and strict save section. Registered the shared runtime and save owner; it uses the ledger's actual world epoch and exposes a small single-subject helper without permitting arbitrary runtime outcome types.
- Unity compile r140 exited 0 with zero C# errors and no fatal marker after the shared contract registration. No runtime/fault/save PASS is claimed from this compile-only gate.
- Began three bounded non-overlapping source integrations in parallel: service-mode change, character proficiency award and exterior visitor reception. Main retains all multi-owner transaction/save design and final verification.

## Body-health lifecycle transition start — 2026-09-20

- Began the next reviewed producer after immutable inventory r38. The required knowledge-base-first query failed stale with 915 failures; source inspection replaced it without rebuilding generated indexes mid-edit.
- Mapped the sole publisher and six consumers, all `SyncLifecycle` call sites, the V5 body-health save owner, and existing outer rollback paths. Confirmed that observer-only recording would violate the approved prepare/domain+outbox commit contract.
- Opened SOURCE_EDIT for a saved downed/recovered transition owner that composes with combat, work-accident, environmental-fire and captivity parent transactions. No gameplay source has been changed for this new batch yet; no PASS is claimed.

## Captivity interaction outcome — 2026-09-20

- Closed the final batch gate after compile r139, focused r77 (47/47) and full-world r35 (80/80) passed on current source. Report SHA-256 values are `23A90063727F72CFBF2FF45E8397F7A26F001C00D84A26A9A38895E100378D2C` for focused r77 and `07486C63625FD906537F6EAA6E76F2E96ED108C07FCCA5767194FF90BB2C3FFF` for full-world r35.
- Promoted only `CaptivityInteractionResult` to `connected` with `CaptivityInteractionOutcomeReceipt` as replacement authority and the exact runtime/bridge/test/evidence paths. No other reviewed row was added or removed.
- Inventory r37 failed closed at `unreviewed=1` because the scanner treated `CharacterPreparedAggregateDamageReceipt` as a pre-existing result. Preserved r37, classified that Phase80 staging token in the explicit infrastructure allowlist, and generated new immutable r38.
- Independent r38 verification: source start/end digest both `1fcf37718a1c45892e1e0f67c9965ef15654f1b68fc27e590f1d38fae27de150`; 323/323 reviewed, 38 connected, 57 pending, authority 0, unreviewed 0, infrastructure 93; r36→r38 reviewed delta exactly one row; 10/10 artifact hashes match. No Unity run, MCP use, approval change, commit or push occurred in this manifest-only closeout.

## Captive ransom outcome — 2026-09-20

- Added `captivity.captive-ransomed`, its exact receipt/bridge/descriptor, production owner integration, saved recovery fields and focused regressions. Compile r132 exits 0 with zero C# errors; focused r75 passes 46/46.
- Full-world r34 reports PASS for 80/80 sections, baseline restoration and canonical equality with zero report warnings/errors. Report SHA-256: `6FF4E86C20468DDBAB39EFFF5BE3A8A3BE4609461919FFB18A69A3D87F58ADBD`.
- Unity reached `Cleanup mono` but its batch PID remained alive. After verifying the exact PID 21200 tree belonged only to this run, the root and four child processes were stopped; none remain. Final log SHA-256: `15092F310EE710690B78727EC1B1483F6483DD055BB3A8609609A5572B233F6C`.
- Group 2 now classifies only `CaptiveRansomedEvent` as connected with the r75/r34 evidence. Immutable inventory r36 is stable at 323/323 reviewed, 37 connected, 58 pending, 0 authority fixes and 0 unreviewed; all 10 artifact hashes match. No Unity or dungeon-player MCP was used.

## Phase80 resume — 2026-09-19

- Final r36 verification: 66/66 recorded source/meta hashes match, Unity processes 0, scoped Unity diff whitespace check passes. NarrativeAI is not a Git checkout, so its attempted HANDOFF git-diff check was unavailable; direct whitespace/conflict-marker and current evidence-path checks were used instead. Source edits remain stopped after the verified run; only planning/handoff text was updated.
- r36 PID 33864 exited 0, Unity process count 0, current C# errors 0, focused 13/13 groups PASS. Added real reward/task/physical/outbox joint completion, exact pending-receipt/task cleanup, completion typed facts/projection, modern restore identity checks and durable allocation high-water. Agent gateway/descriptor/actual-owner fixture changes were reviewed; main corrected definite-assignment risks, type accessors, Korean possessive wording, source-definition restore validation and the missing fourth subject link, and required observation of actual reward/source state at the injected failure boundary. r34 exited 1 on two CS1061 errors; r35 compiled but 3 new groups failed at direct-participant-subject-link-missing; all logs and 62-source inventories retained. Final r36 has 66 source/meta hashes. Codex prepared-state/encoding/outbox JSON tests are not full Codex/material save integration. Legacy allocation history and split-commit pending state explicitly warn/block unsafe operations and remain an unfinished compatibility gate. No MCP, training, release, commit or push.
- Resumed after AGENTS-only maintenance (no ledger progress in that reply); r33 remains a verified regression-discovery turn. Unity absent, SOURCE_EDIT open. Added reward preparation/publication/rollback and actual physical participant/acknowledgement integration; agents extend the exact gateway and knowledge receipt/actual-owner fixtures. No PASS yet. Initial combined inspection outputs were truncated and narrowed; catchup is non-authoritative older context. Attempt to create a new bounded agent hit the session thread limit, so existing agents were reused; no extra thread was created.
- Revalidated r32 (48/48 hashes) and absent Unity; previous goal turn was verified implementation progress. Main designed the two after-effect probes, Terra added only the bounded fixture, main reviewed and added actual-count failure diagnostics. r33 PID 39180 exited 1 with zero C# errors: all existing 9 groups PASS, new commit-gap group FAIL on both reward duplication (2 applications/20 pressure) and acknowledgement stall (task count 1). Recorded 48 hashes and the exact two test-only changes in r33/knowledge-commit-gap-source-evidence.json. No production fix is claimed; main inspected the real physical participant and Codex/region owner boundaries, and retained the strict failure gate for the next edit batch. No MCP, training, release, commit or push.
- r32 Unity PID 31052 exited 0; current C# errors 0, focused 9/9 groups PASS, Unity processes 0. Ten real-save-service integrity groups verify JSON roundtrip, same-sequence progress/wear corruption, section/manifest-row swaps, metadata/root corruption, explicit legacy warning without resealing, malformed versions, preflight edits/resealing/downgrade and empty collections. Main tightened capture assertions to require the integrity error, rather than any exception. Existing checkpoint tests also pass. Recorded 48 SHA-256 entries in r32/research-source-evidence.json. The save integrity fixture uses a recording registry, not actual gameplay restore; the checkpoint fixture uses a test save service. Two literal wildcard source reads were invalid under Windows and replaced by directory/glob-filter searches; no code or evidence was derived from the failed reads. No commit/push/MCP/training/release.
- Resumed after the AGENTS-only response (no ledger progress); Unity processes 0, main EDIT_READY. Current worktree contains unfinished snapshot sealing, not a new compiled PASS. Knowledge-base query is stale (650 failures); current sources remain authority. Finishing cold-path payload/root binding, explicit legacy disclosure and preflight mutation rejection, with an independent bounded save-service test helper; no gameplay, balance, approval or training gates are relaxed.
- r31 PID 16560 exited 0; C# errors 0, focused 7/7 groups PASS, Unity processes 0. The research transaction group now includes sequence-join capture/restore tests (10 groups total); runtime composition also passes. Recorded 40 SHA-256 source entries in r31/research-source-evidence.json. Documentation/handoff refreshed, full producer/save/UI/Player gates retained. No commit, push, model training, release promotion or MCP use.
- After r30, opened a new edit batch for the demonstrated research/ledger sequence-join gap. Registered the domain validator for both capture and restore, with non-mutating mismatch/missing/duplicate/retired-identity tests. Frozen for r31 including the existing runtime-composition check because save registration changed. This is not the full equipment/progress state-digest join. One handoff patch expected a partial line and failed atomically; reapplied against its exact full line.
- r30 PID 21820 exited 0; no Unity process remains, C# errors 0, focused 6/6 groups PASS. Saved 38 source hashes in r30/research-source-evidence.json. SOURCE_EDIT is reopened only after terminal confirmation. Pending knowledge work remains completed work, resumes physical/final reward handling once, and does not produce a false reward-completion receipt. Full save-generation join, generic reward atomicity, real UI and Player performance remain open. No manifest/approval/training promotion.
- Tool-only corrections in this batch: missing guessed source paths were located with rg, a nonnumeric Select-Object count was replaced with an integer, and an out-of-order planning patch was reapplied in file order. These caused no source mutation or failed Unity test. Catchup history was unrelated and ignored.
- Equipment receipt regressions READY/FROZEN and reviewed (5 groups); main fixed one definite-assignment risk before compiling. Added knowledge transaction rollback/capacity/pending-JSON/full-bar assertions and 8 real work/tick fault-injected groups, including physical commit-then-throw recovery before material redelivery. All writers frozen for create-only r30; no tests claimed passed yet. Catchup ended on unrelated old export history and was not used as current evidence.
- Resumed after document-only maintenance. No Unity process exists; current source contains unfinished equipment/knowledge work beyond r29. Main completed the knowledge routing and post-progress pending-finalization boundary and equipment projection; Terra owns isolated equipment receipt tests. Initial combined output was truncated and reread narrowly; two guessed source paths failed and were replaced by rg-discovered paths. No PASS or manifest promotion claimed.
- New goal continuation: r29's 19 hashes match and Unity process count is zero; previous turn is verified implementation progress. Main EDIT_READY for equipment identity/wear context. Reusing the research agent only for a read-only knowledge-processing owner map; no repeated full instructions or stale-index rebuild.
- r29 PID 34628 exited 0, current-source C# errors 0, 4/4 focused groups PASS: research transaction (8 groups), runtime research, durable-use post-commit faults (5 new cases) and research equipment adapter. Warnings from injected cleanup faults are deliberate and assert an explicit PostCommitFaultCode. All Unity processes are gone. Recorded 19 source SHA-256 values in r29/research-source-evidence.json; r10 remains historical and no manifest/approval/training/release promotion was made. Next: exact equipment receipt context, combined save join and actual leap/UI path; broader producer/evidence/performance gates unchanged.
- Added explicit durable-use PostCommitFaultCode and protected only post-effect close/readback. Failures keep committed wear/effect and report pending cleanup instead of false failure; fatal process errors are not treated as recoverable. Terra supplied five bounded fault regressions, main reviewed them. Added special-outbox JSON restore/re-delivery check and removed the now-unused research completion formatter. All writers READY/FROZEN for r29 (research + durable-use + actual research equipment adapter suites).
- Research-focused r28 PID 3060 exited 0, C# errors 0, 2/2 groups PASS including 8 transaction groups. This proves the new positive/negative/zero leap receipts, exact trait rollback, capacity deferral, fixed-hash retry, trait save/replay and immediate completion in controlled fixtures. Reopen SOURCE_EDIT after terminal confirmation to inspect nested durable-equipment completion; full game save/UI is still not proved.
- r27 PID 44556 exited 1 at compile: CS0165 in the new stale-preparation regression (`second` was declared after a short-circuit). No tests ran. Confirmed Unity absent, reopened SOURCE_EDIT, initialized the variable explicitly and froze for create-only r28. Production source is unchanged from r27; failure log preserved.
- Main sources/tests READY/FROZEN; read-only agent finished. Enter UNITY_VERIFY for create-only research-focused r27. Eight transaction groups include the three new special-path groups, plus the existing runtime work/immediate-completion suite. No PASS claimed before execution.
- Implemented special research preparation/joint commit and removed direct runtime completion/leap bypasses. Extended receipts compatibly with explicit cause/risk/hash/fraction/aftermath facts; old ordinary receipt shape is unchanged. Added focused deterministic outcome, lower-clamp, rollback, capacity, trait JSON and immediate-completion checks. Verification is not yet run; whole research/equipment/save/UI gates remain open. A guessed wildcard read failed harmlessly and was replaced with scoped rg discovery.
- Resumed full ledger goal after the AGENTS-only path edit (no ledger progress). Current r26 source inventory matches all 12 files; Unity is absent. Catchup again points to unrelated old export work and is not current evidence. Main is EDIT_READY; research agent performs only a bounded leap-state map. Oversized combined reads were truncated; required instruction tails and narrow source ranges were reread.
- Research-only r26 PID 3676 exited 0; no Unity process remains, C# errors 0, research joint-transaction and runtime-work-path both PASS. The first includes 5 fault/save/query groups; r25 already passes the 19 base groups with unchanged production source. Added r26/research-source-evidence.json (12 source SHA-256 values; current Git base b48bc859f452806635d51f25862d56c49baf6ebb, dirty worktree). Whole research remains pending because leap/debug and nested equipment/final save/UI evidence are open; no manifest promotion, training or commit/push.

- Focused r25 PID 38588 exited 1: 20/21 PASS; all 5 new transaction subgroups PASS, only the existing approved-WU fixture fails. It retained a mutable progress reference across the new detached aggregate publication; corrected the fixture to re-query the authoritative state and verify the old snapshot remains unchanged. Production sources unchanged from r25. Source frozen for research-only r26 to avoid repeating unrelated PASS groups.

- Focused r24 PID 48000 exited 1: no C# errors, previous 19 groups PASS, both research groups FAIL. Root cause in new descriptor: it paired subject and participant rows by index although the core sorts subjects by stable ID. Fixed identity-based coverage after terminal process confirmation; also stopped claiming an automatic completion is a user pin (completion anchor/memory policy remain). Assertions now include failure detail. Frozen for r25; r24 preserved.

- Research normal project/legacy work now calculates detached research/shop states, reserves the exact receipt, publishes in a rollback boundary and commits outbox before notifications. Added saved sequence, typed capacity deferral and 5 focused fault/save/query groups plus existing runtime research scenarios. Receipt review corrected day-zero, double delta, project-specific consolidation and Korean sentence particles. Sources frozen for create-only focused r24; no pass claimed before execution.

- Resumed research integration after the AGENTS-only turn (no ledger progress). Unity is absent. Source map completed read-only; receipt/adapter work delegated with exclusive file ownership. Catchup returned unrelated older export work again; current source wins. Corrected truncated reads and nonexistent guessed source paths through exact source discovery. Knowledge-base query is stale (565 failures); no rebuild during source work.

- Updated current planning summaries and NarrativeAI/HANDOFF.md with r23 and r7–r9 evidence boundaries; human/training gates unchanged. Scoped Unity diff whitespace check passes. NarrativeAI is not a Git repository here, so its attempted git-diff check was unavailable; checked HANDOFF content/whitespace directly instead. No staging, commit or push.
- Defense r9 exited 1 at combat elapsed 110.546s, before medical phase: front collapsed after two exchanges and a new engagement appeared. Full capture still PASS; recovery remains unverified. No timeout or combat-assertion relaxation/retry was made after this failure. Current-source compile has no C# errors; r23 remains 19/19 focused PASS. All Unity processes are gone. A planning-file write briefly failed; free space/attributes were checked, then a smaller exact patch succeeded without deleting/replacing files.
- Defense r8 exited 1: combat/damage/defeat/save all pass, medicine consumed but first course reaches only 53.554/58.08 before the spawn-relative 180s deadline; no recovery PASS claimed. This differs from the pre-fix completed-course/no-healing defect. The verifier now measures combat and post-combat medical phases separately at the existing 180s each and reports both elapsed durations (bounded combined journey, no gameplay rate changes). r8 and its successful final save preflight remain immutable. Visitor preparation empty-evidence exception remains a separate open P0.
- Focused r23: Unity PID 34828 exited 0, current-source compilation has no C# errors, all 19 groups PASS. Human/Slime surface healing now spends the canonical budget exactly once and survives repeated snapshot queries. Live r8 tests recovery with the original medicine/facility numbers and 180-second limit.
- Medical red-r1: Unity PID 6572 exited 1 with the expected Slime treatment-budget assertion (missing 9.999996 after 5 healing). Corrected shared surface healing and clarified verifier current-work versus historical-max output; AI-resumed report now also requires Active and released rescue ownership. No authored medicine/facility values, timeouts or save schema changed. Source frozen for r23.
- Defense r7 exited 1: 14/15 rows PASS; defeat rejection, event-driven downed terminal and post-terminal/final capture PASS (matching captivity/loadout owner). Medical recovery remains FAIL after two doses. Live read-only queries confirmed natural Slime casualty and applicable staff treatment efficiency; two diagnostic snippets initially used an invalid namespace/property and were corrected without game-state mutation. Evidence: Artifacts/QA/GameplayOutcomeLedgerPhase80DefensePlayMode-20260919-r7/. No save slot writes.
- Focused r22: Unity PID 31220 exited 0, no C# compile errors, 18/18 groups PASS including lifecycle-spoof rejection and body-health defeat authority. Evidence: Artifacts/QA/GameplayOutcomeLedgerPhase80Focused-20260919-r22/focused-runtime-report.json. Begin new live r7; the independent medical gate is not yet claimed fixed.
- Added body-health-authoritative defeat preflight before encounter rewards and direct suppression, and idempotent repeated suppression that preserves the living captive. Factory binds the existing body-health query; lifecycle-only downing is rejected. Live fixture now labels controlled body injury after real exchanges and checks refusal, event-driven terminal and full in-memory save preflight. Related WIM capture fixture now supplies a real body injury instead of forcing a healthy terminal. Source frozen for focused r22. Scoped reads corrected absent guessed paths/glob operands; no source change from those failures.
- Resumed after the AGENTS-only turn (no ledger progress in that turn). Revalidated r6 terminal FAIL at 2026-09-19T02:01:34Z and absent Unity process. Supplemental save diagnosis proves living intruder ownership loss, not a missing actor. Main opens SOURCE_EDIT; medical source tracing delegated read-only. Planning skill catchup again returns unrelated old export text; current source/report wins.
- r6 pre-engagement read-only capture PASS, with the live active intruder owning its loadout and no captivity owner. Observation saved under r6/pre-engagement-capture.json; no slot was written. This confirms the production Tier-0 fixture initialization removes the prior 27/29 layout error before combat.
- r5 exits 1 at 2026-09-19T01:54:59Z: combat/ledger PASS, natural combat-down exposed the verifier's unconditional two-healthy-workers injury setup. Adopted natural medical journeys, preserved the full stabilization/carry/treatment/recovery gate. During r5 the concrete autosave service was detached via CLI before a day-triggered write; this is now a Phase80-only fixture action with independent in-memory capture diagnostics. Follow-up live capture command found no pipeline after the process had already exited; no result was claimed and no retry/restart was made for that observation failure.
- Defense r4 exits 1: 12/13 report rows PASS, medicine consumption and treatment now observed, but AdditionalTreatmentRequired ran out of the fixture's single seeded dose. Corrected the fixture to use production medicine ranking and bounded repeat supply (original two-per-kind inventory) and production Tier-0 initialization. No medical gameplay rates, health thresholds, capacity or timeout changes.
- Corrected the defense medical fixture from an orphan legacy destination to the runtime-owned versioned one-item claim. No gameplay supply authority, medicine balance or timeout was changed. The next run is r4; r3's visitor-generation and autosave defects remain tracked separately.
- Defense r3 completed with exit 1 at 2026-09-19T01:43:04Z: 11 report rows PASS including actual 3 exchanges/2 acknowledged damage outcomes; medical treatment and terminal recovery FAIL (SupplyUnavailable, seeded=14). Runtime startup also exposes empty-evidence visitor preparation. CLI read-only editor_status/eval confirmed the same live run was playing/recovering, not stalled; no restart/timeout extension/MCP calls occurred. Source editing resumed after PID 32028 terminal.
- Scoped source discovery corrected absent guessed registration/Models/Character/Core/Editor paths and PowerShell literal glob patterns to actual rg-resolved sources. No files were changed by those failed reads.
- Focused r21 passed 17/17 groups on current source; Unity PID 1904 exited 0. The corrected lifecycle regression verifies no draft/evidence fabrication before restore and generation resumes after a real test narrative fact. Evidence: Artifacts/QA/GameplayOutcomeLedgerPhase80Focused-20260919-r21/focused-runtime-report.json. Defense r3 is the next actual PlayMode check, not evidence of a production New Game readiness pass.
- Goal continuation after AGENTS feedback: the preceding document-only turn was no ledger progress. r20 is now inspected: 16/17 groups pass; Unity log terminates with code 1 and Unity.exe is absent. The old PowerShell Start-Process -Wait wrapper remains waiting on descendants, not an active Editor. New launches will wait on the Unity process itself.
- Corrected unpublished-composition fixture ordering before injection; r20 evidence is preserved and r21 is the next create-only report. Session catchup again returned unrelated older export work; current source/report remains authority.
- Previous turn handled the requested AGENTS cleanup; no gameplay implementation progress was claimed for it. Revalidated the next safe action against current files and absent Unity processes.
- Knowledge-base query DefenseCombatExecutor/code returned stale (549 failures): content digest 3dfba6672befaaacf7f227031472d7f526b4c90158c9f117e4abeaac364b61c8, system digest 81f7b9f54400f4f3916e0b46e3177bef3b150ae41d68d310cd68b449264eda18. Investigation uses original verifier/source, not stale index results.
- Entered UNITY_VERIFY for create-only defense r2. Main is sole Unity owner; no MCP tools are used.
- r2 completed with exit 1 (PID 35148): party initialization threw for missing meaningful skill evidence; no combat was executed. Source edits began only after terminal process confirmation.
- Added unpublished/detached configuration deferral, a no-fabrication/resume regression and full exception diagnostics. Prior broad reads were truncated and replaced with targeted method reads; guessed preparation-model/coordinator paths were replaced by rg-resolved source locations. A disposition projection used wrong property names; the exact CombatAttackResult entry was then read without projection.

## AGENTS simplification: 2026-09-19

- Main owns guideline restructuring; Terra performs a read-only inventory of existing audit commands.
- Initial combined output was truncated; the stored skill text was read separately. Missing docs/dev and .planning directories were confirmed; existing root planning files are reused.
- Changes are documentation-only; gameplay sources, QA evidence and approval gates are outside this batch.
- Tool inventory: knowledge-base query yielded no completed freshness result (unverified). The V27 verifier does not parse --help; that invocation ran its read-only body and failed on missing HamburgerStore.asset in its source inventory. No pass was claimed and no generated artifacts were written by that verifier.
- Session catchup returned an older unrelated export session; current user feedback, source diff and this documentation task remain the authority.
- Completed root reduction and three conditional references; documentation navigation updated. Six documentation files have 82 valid local links/anchors, with zero broken links; git diff whitespace check passed.
- New docs/dev files initially matched docs/* in .gitignore. Added a Markdown-only exception so the manuals can be committed; no staging/commit/push performed. A direct .git/info/exclude probe was unavailable in this checkout; check-ignore identified the project rule without that path.

Updated: 2026-09-18

The complete pre-cleanup progress log is recoverable from Git commit
`6e77756f8909bcb23cc91f2698e5129e90eec123` (original SHA-256
`b697307794b0b166e7f7cc66cf1a938443e0683b2940d4915848c1a1d4252e47`).

## Latest verified implementation state

- Unity 6000.3.8f1 create-only current production-source compile/focused run:
  `Artifacts/QA/GameplayOutcomeLedgerPhase80Focused-20260919-r26/unity-focused.log`
  reports exit code 0 and zero C# compile errors.
- Gameplay-outcome verifier suite: 17/17 PASS.
- Focused runtime evidence covers the 19 base groups in r25 and 2 research groups in r26,
  including composition, owner transactions, defeat authority and Human/Slime surface healing. Live verification is tracked above;
  it remains distinct from full producer/UI/save-roundtrip/Player evidence.

## Documentation maintenance

- The obsolete `docs/game-design/plan.md` was removed after its only reference was redirected.
- The complete Unity worktree through the prior integration batch was committed and pushed as
  `6e77756f8909bcb23cc91f2698e5129e90eec123` on `codex/wim-implementation`.
- Ignored `.planning/` session scratch was verified as untracked and non-authoritative before
  being moved to the Windows Recycle Bin: 59,946 files, 19,707 directories,
  588,764,070 bytes (561.49 MiB). The removal is recoverable from the Recycle Bin.
- The former cumulative root logs were reduced to current state. Their original sizes and hashes were:
  - `task_plan.md`: 757,239 bytes, SHA-256 `868e39f8a9e48656745e6a6e7b1d069c170b2976e9357f1b08407e7d9e8bb19e`
  - `findings.md`: 1,694,488 bytes, SHA-256 `1e605813536179deecb5e42325ee28d10a39bce3914779b2a56e39ed68a72e33`
  - `progress.md`: 1,413,578 bytes, SHA-256 `b697307794b0b166e7f7cc66cf1a938443e0683b2940d4915848c1a1d4252e47`

## Next action

Continue research leap/debug and nested-owner closure, and isolate live medical recovery evidence;
address remaining production exceptions and producer gaps (including cover/miss/wildlife attacks). Close consumer,
UI, persistence and Player evidence before returning to the 100-row pilot.

## Production command outcome integration — 2026-09-19

- Revalidated the production-bill DTO, aggregate/session, codec, persistence capture and runtime command source directly; no generated knowledge-base row was used.
- Added save v25 `nextCommandOutcomeSequence` and `pendingCommandOutcomes`, including deterministic capture, frozen-context validation and legacy-zero sequence normalization.
- Added `production.command-applied` receipt/adapter/descriptor/bridge with frozen facility names, location, before/after facts, low-salience facility memory and Korean projection.
- Registered the adapter/descriptor/committer and connected minimum quality, priority, suspension, stock policy, order mode, distribution policy, worker policy and emergency-worker commands through prepare → mutation → durable commit, with exact rollback on definite rejection and no sequence consumption for no-op requests.
- Connected AddBill before its child input claim and RemoveBill before irreversible child work. Successful irreversible mutations persist a frozen outbox row first; delivery rejection or exception leaves it pending for ordered restore retry without replaying the domain command.
- Connected stock-sensor physical admission, acknowledgement and removal as three exact transitions, including deferred finalization detection. Missing facility/outcome preparation is fail-loud rather than silently treated as recorded.
- Added `ProductionCommandOutcomeDebugScenarios` and focused-runner coverage for adapter/descriptor registration, facility/global queries, frozen facts/location/name, Korean josa projection, canonical replay, drift rejection and ledger save round-trip.
- Added owner regressions for add/remove, reversible rejection/no-op, pending-outbox JSON retry, invalid frozen receipt rejection and exact stock-sensor transition count. r60 exposed a locked-research test setup; r61 identified `ProductionResearchLocked:research:forestry:sawmill`; the fixture now explicitly represents an already-unlocked recipe without weakening production research rules.
- Unity compile r108 passed with zero C# errors: `Artifacts/QA/GameplayOutcomeLedgerPhase80Compile-20260919-r108/unity-compile.log`. The diagnostic-only r106 compile failure is retained; r107 and r108 pass.
- Focused runtime r62 passed 41/41, including `production-command-outcome` and `production-command-owner-transaction`: `Artifacts/QA/GameplayOutcomeLedgerPhase80Focused-20260919-r62/focused-runtime-report.json`.
- Full world-save r25 passed 79/79 with production save v25 and the durable outbox: `Artifacts/QA/GameplayOutcomeLedgerPhase80FullWorldSave-20260919-r25/full-world-round-trip-playmode-report.txt`. Its Unity log later emitted a shutdown autosave warning about destructive-drain checkpoint topology; the in-memory roundtrip report itself passed and that separate shutdown concern is not concealed.
- Inventory r27 is stable and hash-valid: 323/323 reviewed, 95 Record / 228 NonResult, connected 32, pending 63, authority 0, unreviewed 0, 10/10 artifact hashes match. r26 is retained as an incomplete scanner-classification run; r27 classifies the two new owner-facing production outcome contracts as Phase80 infrastructure.
- SOURCE_EDIT reopened only after Unity exited. Unity MCP and dungeon-player MCP remain unused.

## Industrial infrastructure command outcome integration — 2026-09-19

- Revalidated zero Unity processes and recovered the previously truncated edit: 13-kind contracts, shared aggregate and owner runtime were present. Replaced the hidden prepared-token cast with an explicit immutable source property.
- Added the typed `infrastructure.command-applied` adapter, descriptor, Korean perspective projector and prepared owner bridge; registered it with gameplay outcomes.
- Added the dedicated strict `world.infrastructure-command-outcomes` staged save section and registered the shared sequence/pending-outbox runtime as the sole owner.
- Routed power, fluid and automation command mutations through prepare → mutation → reversible ledger commit with exact aggregate rollback and pre-prepare no-op detection. Existing direct fixture constructors remain available; production injection supplies the outcome transaction.
- Reworked conveyor command state toward the same reversible boundary. Replaced transient overflow approval with payload-owned runtime/save state, bumped conveyor save v4, and queued the approval outbox before any physical discharge attempt.
- Verification has not started. Source remains `SOURCE_EDIT / EDIT_READY`; tests, static review, Unity compile/focused/full-save evidence and manifest promotion are still pending. Unity MCP and `dungeon-player` remain unused.
- Added focused bridge/owner/save validation, invalid frozen-row and duplicate-revision regressions, plus conveyor approval JSON round-trip coverage. Updated the immutable next focused path to r63.
- Static diff and symbol checks pass. Unity process count was zero; all source writers are now `READY/FROZEN` and the window changes to `UNITY_VERIFY` for the first current-source compile.
- Compile r109 ran against current source and exited 1. It found eight unique errors: automation aggregate/mutation API accessibility across assemblies and invalid `InfrastructureStatus ==` comparisons in automation/fluid. The process is terminal, evidence is preserved, and `SOURCE_EDIT` reopened for the narrow fix.
- Replaced the automation aggregate leak with an opaque public mutation token and changed both status no-op comparisons to `.Equals`. Targeted static checks pass; sources are `READY/FROZEN` for compile r110.
- Compile r110 exited 1 with exactly one editor-only error: the new infrastructure outcome scenario lacked the existing `DungeonStory.Foundation` import for `GameEventBus`. All r109 production errors are absent. r110 is preserved and the source window reopened for that import only.
- Added the missing import, confirmed zero Unity processes and scoped whitespace cleanliness, and froze source for compile r111.
- Compile r111 exited 0 with zero C# errors. Focused r63 exited 0 and passed 42/42; `infrastructure-command-outcome` covers frozen facts/query/Korean projection/replay, owner rejection sequence rewind, pending retry, JSON restore, invalid row/duplicate rejection and save-section dependencies. MCP usage flags remain false.
- Full-world save r26 passed 80/80 with baseline restoration, canonical equality and zero report warnings/errors: `Artifacts/QA/GameplayOutcomeLedgerPhase80FullWorldSave-20260919-r26/full-world-round-trip-playmode-report.txt`. The report and cleanup log were complete, but the batch process remained alive without further log writes during final shutdown; only that verified batch PID was stopped. The separate shutdown autosave warning about destructive-drain checkpoint topology remains disclosed and does not change the round-trip PASS. Source is now `SOURCE_EDIT / EDIT_READY`; actual producer/no-op live coverage and inventory promotion remain open.
- Resumed from current source with Unity absent and the main agent as sole writer. Re-read the approved 826-line ledger contract and current planning files. Source inspection confirms r63 uses fake industrial outcome producers; the existing live industrial verifier touches only a subset and does not prove no-op sequence stability. The next edit is a real-runtime producer/no-op regression, not another bridge-only assertion.
- Added a dedicated real-DI infrastructure-command PlayMode mode and batch facade. It places authored breaker/transfer fixtures, drives all 13 power/fluid/conveyor/automation mutation kinds, verifies one canonical ledger fact and one owner revision per real change, and verifies the repeated no-op preserves domain JSON, owner sequence and outcome absence. The same batch isolates real save/profile files and restores the whole save-registry baseline. Also made current-version outbox rows reject a null collection and added stale/null restore regressions. Static kind coverage is 13/13 and scoped whitespace checks pass; no Unity result is claimed yet.
- Compile r112 is retained as immutable failure evidence. It exited 1 with one editor-scenario error: `priorityBefore` could be read before definite assignment (`CS0165`). Unity exited cleanly; `SOURCE_EDIT` reopened and the local snapshot is now initialized explicitly before the guarded lookup. No runtime result is claimed from r112.
- Compile r113 exited normally with Unity return code 0 and zero C# errors. The focused runner evidence path is advanced from immutable r63 to r64 before exercising the stricter infrastructure-outbox restore regressions.
- Focused runtime r64 exited 0 and passed 42/42 with zero C# errors, including the stricter stale/null infrastructure outbox restore rejection: `Artifacts/QA/GameplayOutcomeLedgerPhase80Focused-20260919-r64/focused-runtime-report.json` (SHA-256 `F29062DDF0B08599999BF1263F95A8F6E97C47D5EE192FF66E4757733D6468FA`).
- Industrial command PlayMode r1 failed before entering PlayMode because the facade called `File.Delete` for `Temp/IndustrialInfrastructure/playmode-live-report.txt` before its parent directory existed. The immutable `unity-playmode.log` is retained. The facade now creates that report directory first, and the next evidence path is r2; no gameplay result is claimed from r1.
- Compile r114 exited 0 with zero C# errors. Industrial command PlayMode r2 then reached the real world but failed before command execution because no contiguous free 12x3 fixture region existed; cleanup also compared the gameplay ledger's deliberately advanced restore epoch as raw bytes. r2 is retained as FAIL. The r3 verifier now publishes a temporary 12-column interior strip through the real grid manager when needed and accepts only the same gameplay-ledger epoch normalization already used by the full-world verifier (plus the pre-existing tiny meta-clock rounding allowance); every other save field remains exact.
- Compile r115 is retained as FAIL with one `CS1026` from a missing closing parenthesis in the newly factored save-envelope comparison. The condition is rewritten into an explicit `payloadMatches` calculation before the identity checks; no runtime verification was reached.
- Compile r116 exited 0 with zero C# errors. Industrial command PlayMode r3 placed all 41 authored fixture objects in a temporary 60→72-column grid, reached all 13 real command changes, and restored the world canonically, but failed the final automation-maintenance no-op: restored `InfrastructureStatus` had `Code=None` with a null parameter array, so whole-struct equality treated it as different from `InfrastructureStatus.None`. The automation and analogous fluid no-op guards now compare the authoritative status code; next evidence is focused r65 and live r4.
- Compile r117 exited 0 with zero C# errors. Focused runtime r65 passed 42/42 (`Artifacts/QA/GameplayOutcomeLedgerPhase80Focused-20260919-r65/focused-runtime-report.json`, SHA-256 `CBEC8BAEB377D874650801BD40E853537E626CB39061290C778B3100C14A50D6`). Real-DI industrial command PlayMode r4 passed all 13 changes and 13 repeated no-ops with owner revision 1→14, preserved all four real persistence files, and restored the world canonically (`Artifacts/QA/GameplayOutcomeLedgerPhase80IndustrialCommands-20260919-r4/producer-runtime-report.txt`, SHA-256 `0A3B158FACC6084132A845FF967CA63177EE0B4601CD8228C5E617F50461F672`).
- Full-world save r27 passed 80/80 with canonical baseline restoration, character progression contracts, and zero report warnings/errors: `Artifacts/QA/GameplayOutcomeLedgerPhase80FullWorldSave-20260919-r27/full-world-round-trip-playmode-report.txt` (SHA-256 `3DD309A2CAE27623ACB41D76F0D3C1B75D79923E5B63414B1E79AF2E08EACC7F`). Inventory r28 is retained as an incomplete scanner-classification run: it promoted only `InfrastructureCommandResult`, but misclassified the two new owner-facing infrastructure outcome contracts as pre-existing and therefore reported `unreviewed=2`. The scanner's explicit Phase80 infrastructure allowlist now includes those two contracts; regenerate as r29 rather than modifying r28.
- Inventory r29 is stable and hash-valid: 323/323 reviewed, 95 Record / 228 NonResult, connected 33, pending 62, authority 0, unreviewed 0, stable source digest, and 10/10 matching artifact hashes. The reviewed-disposition delta from r27 is exactly `InfrastructureCommandResult: transaction-integration-pending → connected`; r28 remains immutable incomplete evidence.

## Work-completion identity outcome integration — 2026-09-20

- Added a typed `character.work-identity-applied` receipt, adapter, descriptor, Korean projector and prepared owner bridge. Direct events now receive a persisted character-owned sequence; durable harvest keeps its existing producer identity. Only applied character reactions are recorded, not the upstream work-domain result.
- Added `EffectsAppliedWithOutcome` cursor provenance, exact replay reconciliation, legacy backfill and definite-rejection rollback across identity state, mood and progression. Terminal unavailable recipients still retire without fabricating an outcome.
- Compile r118 is preserved as FAIL: three founder-trait audit fixtures lacked the new bridge constructor dependency. Those fixtures now build the real registry/ledger/name resolver/bridge. Compile r119 then passed; the outcome definition was moved into the narrative bridge boundary and final compile r120 passed with zero C# errors.
- Focused r67 passed 43/43 on final source, including the new outcome query, Korean projection, replay/drift rejection, direct-sequence restore and the existing live adapter fault/replay/absence tests: `Artifacts/QA/GameplayOutcomeLedgerPhase80Focused-20260920-r67/focused-runtime-report.json` (SHA-256 `63E3EBEC310DE6DFC34C292ED25E31727C6760656533B425F0E732E818AFDD8F`).
- Full-world r28 passed 80/80 with canonical baseline restoration and zero report warnings/errors: `Artifacts/QA/GameplayOutcomeLedgerPhase80FullWorldSave-20260920-r28/full-world-round-trip-playmode-report.txt` (SHA-256 `DB5629F6AF50AA19CEBAC52EF854A9AA75BCDDC53C50326E7A89CFEF458FEE4B`). Its verified batch process stopped writing after `Cleanup mono`; only PID 32820 from that run was terminated after the complete PASS report was copied.
- Inventory r30 is stable and hash-valid: 323/323 reviewed, 95 Record / 228 NonResult, connected 34, pending 61, authority 0, unreviewed 0, Phase80 infrastructure 84, stable source digest `737a77863eb991451fe9a1824b2465b5eb9e438d8b09bacc20560d1d2c4c3e59`, and 10/10 matching artifact hashes. The reviewed delta from r29 is exactly `WorkCompletedIdentityEvent: transaction-integration-pending → connected`.
## Captivity interaction outcome integration — 2026-09-20

- Added focused scenarios for the canonical ledger/query/Korean projection, all ten authored handlers, definite rejection, committed delivery-pending, physical-output retry, lethal ordering/death retry, interrogation composition and save corruption/legacy normalization. The focused runner now targets immutable r76.
- Static review also corrected the projected phrase from the invalid `‘회유’을` shape to formatter-owned `‘회유’를`, required all eleven exact metrics in descriptor validation, and made death cleanup retain a frozen result until committed output and body observers finish.
- Compile r136 is retained as FAIL with one `CS0103`: model-only save validation used `Mathf.Max`. Replaced it with `System.Math.Max`; no runtime tests ran in r136.
- Compile r137 is retained as FAIL with one `CS0246`: the focused interrogation fixture lacked the existing `DungeonStory.Operation` import for `EventAlertRequestedEvent`. Added that import; no runtime tests ran in r137.
- Compile r138 exited 0 with zero C# errors. Focused r76 then passed all 46 pre-existing groups and failed only `captivity-interaction-outcome`: the fixture incorrectly required the completed interrogation audit snapshot to disappear. The existing save contract retains the terminal snapshot with both publications complete while clearing only the active attempt; corrected the assertion and advanced the immutable report path to r77.
- Compile r139 exited 0 with zero C# errors. Focused r77 passed 47/47, including the new interaction outcome group and all prior regressions: `Artifacts/QA/GameplayOutcomeLedgerPhase80Focused-20260920-r77/focused-runtime-report.json`.
- Full-world r35 passed 80/80 with baseline restoration, canonical equality and zero report warnings/errors: `Artifacts/QA/GameplayOutcomeLedgerPhase80FullWorldSave-20260920-r35/full-world-round-trip-playmode-report.txt` (SHA-256 `07486C63625FD906537F6EAA6E76F2E96ED108C07FCCA5767194FF90BB2C3FFF`). After the complete report and `Cleanup mono`, the batch process again stalled during teardown; only its verified PID and four exact children were stopped. No Unity-family process remains.
- Added saved interaction attempt/terminal state, the typed `captivity.interaction-resolved` receipt/adapter/descriptor/Korean projection, prepared owner bridge and production registrations.
- Interaction completion now freezes the exact applied result, prepares before captive/body mutation, restores both owners on definite rejection, and retains committed pending delivery for retry without handler re-execution.
- Optional blood/memory output uses the existing idempotent physical-source publisher. Aggregate body damage is staged with the interaction and publishes irreversible observers only after ledger commit; interrogation publication remains the final composed terminal step.
- Added current-save validation and legacy active-interaction normalization for the new attempt/terminal fields.
- Compile r133 is retained with two missing-import errors. Compile r134 is retained with five descriptor errors. Compile r135 exited 0 with no unique `error CS` lines and `Batchmode quit successfully`; focused and full-world runtime evidence are not yet run.
- No Unity MCP or `dungeon-player` MCP was used. No manifest disposition, human approval, training gate, commit or push changed.

## Migrated producer production-entry 57/57 E2E — 2026-09-20

- Added exact aggregate coverage over five domain-grouped production-entry suites. A PASS requires the observed union to equal all 57 catalog kinds with no duplicate, missing or unexpected value.
- Each kind runs success, definite commit rejection with owner rollback, and durable commit with delivery failure/retry. The generic all-57 transaction test remains a separate gate and is not credited as production-path coverage.
- The E2E found and fixed four production defects: invalid meta-run and facility-shop stable-ID separators, wrong faction constructor selection by VContainer, and an unintended required primitive-survival test scheduler.
- Dedicated r18 passes 57/57 (`success+reject-rollback+delivery-retry`), SHA-256 `126AFF7060AF3EBB0B73CCE7430FE596CB614F67DA7E5C84A301C06327202372`.
- Focused r81 passes 49/49, SHA-256 `C865F89BD9F340FEC50A7F50CED9454C6B8A65F9864E67DE34FEE580B80E93B9`.
- Live `GameplayScene` full-world r40 passes 81/81 sections with canonical restoration and zero report warnings/errors, SHA-256 `77484EC98FDA353AAAA2C9277E1DFCCF59DE496357A6AB3BF45BC09D86E570C3`. r38/r39 preserve the two constructor-selection failures. The later Editor-exit autosave topology warning remains a separate disclosed shutdown issue.
- Compile r164 exits 0 with zero compiler-error signatures, SHA-256 `FEAB5E21AD135E25337F9A91E08F4C63EBAE00BFC72A8D94F94367B561297AEE`.
- Unity MCP and `dungeon-player` MCP were not used. The final-export inventory/closure refresh remains separate; no human/training gate changed.

## Read-only implementation re-review — 2026-09-20

- Reviewed production failure paths and independently reviewed all five E2E suites/probes. Knowledge-base lookup was stale (1,107 freshness failures), so current C# was used.
- Recorded CHANGES_REQUIRED with five P1 production defects and test limitations in `Artifacts/QA/MigratedProducerProductionEntry57-Review-20260920-r1/review.md`; updated the current handoff to prevent reusing 57/57 as proof of all-branch atomicity.
- No production source, scene, asset, historical PASS artifact, approval gate, or external state was changed. No new Unity or MCP execution was performed; findings are source-review evidence, not a newly executed red-test report.

## Production-entry atomicity correction — 2026-09-20

- Implemented the first correction pass for all five reviewed paths: joint field-meal/primitive outcomes, precommit/rollback for primitive survival, world-source breakdown-drink rollback, V20 composite owner rollback through outcome commit, and expedition reward/campaign/meta/world/member rollback with return sealing inside the ledger transaction.
- Scoped whitespace/static interface checks pass. No Unity process is running; the main writer is `READY/FROZEN` and the batch enters `UNITY_VERIFY` for compile r165. Runtime evidence has not yet been rerun.
- Compile r165 is preserved as FAIL with three repeated diagnostics from one boundary defect: the offense assembly referenced the internal meta-run aggregate/token methods. The meta owner now exposes a public opaque rollback token while retaining the aggregate itself as internal; `SOURCE_EDIT` reopened only for that correction.
- Before compile r166, bucket-wash capture was moved ahead of item reservation so rollback cannot resurrect an active lease, and breakdown-drink snapshot failures now cancel the prepared outcome. Foul-water rollback restores body/deprivation owners before applying the final exact stat snapshot.
- Compile r166 exits 0 with zero compiler-error signatures and terminal batch success. Evidence: `Artifacts/QA/GameplayOutcomeLedgerPhase80Compile-20260920-r166/unity-compile.log` (SHA-256 `F38C8F28907ADB6896E507CBC3AFE66A919021C18C66E7A33ABD9AF62E163059`). Runtime branch regressions remain to be added and executed.
- Extended the character/survival production-entry suite without changing its catalog denominator: primitive field meal, latrine, and bucket-wash now each run the existing success/reject-rollback/delivery-retry triplet in addition to floor rest. The suite has not yet been recompiled or executed after this test edit.
- Extended the V20 fixture from campaign-only `WorkDelayDays` to a real two-character relationship plus narrative-history effect, and added a successful-expedition branch that mutates reward, meta, campaign and history owners. Expedition reward/arrival state is now snapshotted for both success and failure so return sealing can roll back on outcome rejection. These edits await compile r167.
- Compile r167 exits 0 with zero compiler-error signatures (SHA-256 `7871BCDE63C9A76E422493EFFA15A3C4A6C44C8314D61E91788EFD5AAA34A1FB`). Dedicated r19 is preserved as FAIL because its new offense fixture used a fake arrival runtime whose restore-candidate method returned null; the fixture now uses the real rollback-capable arrival runtime and advances to r20.
- Dedicated r20 is preserved as FAIL after reaching the new primitive-latrine branch: the editor actor fixture lacked the production `AbilityMove` required even when already near the target, so no owner mutation occurred. Added the real movement ability component and advanced the immutable report path to r21.
- Dedicated r21 is preserved as FAIL because the primitive-latrine fixture used default indoor cells without authored floor/hallway occupancy, so the production walkability rule correctly found no safe cell. The fixture now uses explicit walkable exterior-path cells.
- Dedicated r22 is preserved as FAIL after entering the real latrine commit path because the old floor-rest-only filth test double threw on `AddFilth`. The double now returns a valid immutable filth snapshot and the next evidence path is r23.
- Dedicated r23 passes after the primitive field-meal, floor-rest, latrine, and bucket-wash branch triplets. Focused r82 passes all 49 groups, compile r168 passes with zero compiler errors, and live full-world r42 passes 81/81 sections; r41 is retained but invalid because its test filter selected zero tests and only exposed the prior report.
- Added a second `CharacterWaterConsumedEvent` triplet that executes the production deprivation-breakdown world-source routine and compares thirst, hygiene, water and deprivation state across success/rejection/delivery retry. Compile r169 is preserved as FAIL with one editor-fixture type mismatch (`IDungeonItemCatalogProvider` vs `IItemDefinitionCatalog`); the fixture now builds the required definition catalog directly.
- Compile r170 passes with zero compiler errors. Dedicated r24 passes all 57 catalog kinds with the additional breakdown-water success/reject-rollback/delivery-retry branch; the focused runner advances to immutable r83.
- The successful expedition branch now runs through the production return coordinator with a real participant. Its domain comparison includes expedition-recovery stress, level/XP and `ReturnFinalizing`, closing the previously untested coordinator rollback path.
- Compile r171 passes with zero C# errors (SHA-256 `BCF2ACDD78CD652868097F1C207E41E4526D94960F85C0DCF9D35616ECA9D944`). Dedicated r25 passes 57/57 with every added branch (SHA-256 `126AFF7060AF3EBB0B73CCE7430FE596CB614F67DA7E5C84A301C06327202372`). Focused r83 passes 49/49 (SHA-256 `07B8686FAE106A266955B33F1E17793050ECBEAA04C2F789E906874A754207F6`). Live full-world r42 passes 81/81 with canonical baseline restoration and zero report warnings/errors (SHA-256 `427FC5C976D0D93A82FEC671C29847E4BE3DC515139672510D0BF513518AD173`).
- The five P1 production-entry atomicity findings are closed on current source. Final-frozen inventory/closure refresh remains a later export gate; Unity MCP and `dungeon-player` MCP were not used and human/training flags remain unchanged.

## Final source freeze and Git publication — 2026-09-20

- User requested finalization and push of the completed ledger work. The working tree has 326 tracked changes and 1,174 untracked files; 1,026 untracked entries are under `Artifacts`, including an approximately 1.8 GB Player build. Source, tests, tools, docs and compact referenced QA evidence will be committed; transient Player binaries, process IDs and redundant intermediate outputs will not.
- The final-manifest Python suite passes 22/22. The tracked-only `git diff --check` initially reported only line-ending notices; after new files were staged, `git diff --cached --check` exposed Unity-generated blank-field trailing spaces in 20 new `.meta` files. Those spaces were removed mechanically and the staged check now exits 0. Unity process count is zero, so the source remains frozen after compile r171, dedicated r25, focused r83 and full-world r42.
- One inventory-history search used a Windows-incompatible `rg` path glob (`tools/Verification/GameplayOutcomeLedger/*.py`) and failed without changing files. The retry used an explicit directory plus `-g '*.py'` and succeeded.
- Frozen source commit `a88a0482df32c25431b316d5444e45b34d90da73` contains the 475 curated source/test/tool/doc files. A clean sparse worktree scan was used because the first full-worktree attempt hit Windows path-length failures in old nested exports; the failed location was never registered as a worktree. The sparse worktree is clean and contains only the scan inputs.
- Inventory r45 is preserved locally as an incomplete invocation: PowerShell's reserved `$args` variable prevented the seven disposition options from being forwarded, so it reported reviewed 0. Explicit arguments produced accepted inventory r46: dirty 0, stable source scan `598911bdace5d6e63efe9f4bb7dc6cbdb3ee7bb03a966dbd82f1e503d35e765e`, 323/323 reviewed, 95 Record, 228 NonResult, pending/authority/unreviewed 0. `producer-inventory.json` SHA-256 is `F1E193881C16ECBDF21548AA6BE6CB75A64DAF78F80254E96178E6E88D2FE619`; its 10 declared artifact hashes all match.
- The first closure command guessed `inventory.json` instead of `producer-inventory.json` and created no output. r5 then passed but exposed stale evidence constants pointing to r80/r37. The builder was corrected to post-fix focused r83/full-world r42, its 22 tests passed again, and accepted closure r6 passes 323 rows (95 Record / 228 NonResult / 0 ExactSubstitute), 93 registered outcome types, `humanApprovalClaimed=false`, `trainingEligible=false`. SHA-256 is `5274AC45C9E4EDF1496A95A3F0E3D614A4EA1B611661B77A9823741E286B98C1`.
- The immutable Unity r171 compile log retains two machine-generated trailing spaces in player-connection lines so its recorded SHA-256 remains exact. The final staged whitespace check excludes only that log and passes for all source, tests, tools, docs and structured evidence.
