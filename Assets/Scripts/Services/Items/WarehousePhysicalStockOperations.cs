using System;
using System.Linq;

public static class WarehousePhysicalStockOperations
{
    // Count/category consumption was removed in V27. Callers must select exact
    // physical stack slices and commit them through the typed batch boundary.
}
